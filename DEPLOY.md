# Инструкция по деплою проекта CatDogs

## ✅ Шаг 1: Подготовка проекта (ВЫПОЛНЕНО)

Все необходимые файлы созданы:
- ✅ `docker-compose.yml` - оркестрация сервисов
- ✅ `client/Dockerfile` - образ для React приложения
- ✅ `server/Dockerfile` - образ для Express сервера
- ✅ `server/entrypoint.sh` - автоматический запуск миграций
- ✅ Конфигурации Nginx и другие файлы

## 📋 Шаг 2: Настройка VPS сервера

### 2.1. Аренда VPS

Выберите провайдера:
- **VDSina** (Россия) - https://vdsina.ru
- **Timeweb** (Россия) - https://timeweb.com
- **DigitalOcean** (международный) - https://digitalocean.com
- **AWS** (международный) - https://aws.amazon.com

**Минимальные требования:**
- CPU: 1 ядро
- RAM: 1-2 GB
- Диск: 20 GB SSD
- ОС: Ubuntu 22.04 LTS или Debian 12

### 2.2. Подключение к серверу

```bash
ssh root@ваш_ip_адрес
```

Или если используете ключ:
```bash
ssh -i путь/к/ключу.pem root@ваш_ip_адрес
```

### 2.3. Обновление системы

```bash
apt update && apt upgrade -y
```

### 2.4. Установка Docker

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установка Docker Compose
apt install docker-compose-plugin -y

# Проверка установки
docker --version
docker compose version
```

### 2.5. Установка Git (если нужно)

```bash
apt install git -y
```

## 📦 Шаг 3: Загрузка проекта на сервер

### Вариант 1: Через Git (рекомендуется)

```bash
cd /opt
git clone https://github.com/ваш-username/ваш-репозиторий.git catdogs-app
cd catdogs-app
```

### Вариант 2: Через SCP (с локального компьютера)

```bash
# С вашего компьютера
scp -r "C:\Users\potap\OneDrive\Desktop\elbrus-part-time\phase-3\week-2\day-3\CatDog(my)\cat-dogs-self" root@ваш_ip:/opt/catdogs-app
```

### Вариант 3: Через SFTP клиент (FileZilla, WinSCP)

Загрузите всю папку проекта в `/opt/catdogs-app`

## ⚙️ Шаг 4: Настройка переменных окружения

### 4.1. Создание .env файла

```bash
cd /opt/catdogs-app
nano .env
```

### 4.2. Содержимое .env файла:

```env
# Database
DB_USER=postgres
DB_PASSWORD=очень_надежный_пароль_здесь_12345
DB_NAME=catdogs

# Server
PORT=5001
NODE_ENV=production
CLIENT_URL=http://ваш_ip_или_домен
```

**ВАЖНО:**
- Замените `очень_надежный_пароль_здесь_12345` на надежный пароль
- Замените `ваш_ip_или_домен` на IP адрес сервера или домен (например: `http://185.123.45.67` или `http://catdogs.example.com`)

### 4.3. Сохранение файла

Нажмите:
- `Ctrl+O` - сохранить
- `Enter` - подтвердить
- `Ctrl+X` - выйти

## 🚀 Шаг 5: Запуск приложения

### 5.1. Сборка и запуск контейнеров

```bash
cd /opt/catdogs-app
docker compose up -d --build
```

Эта команда:
- `up` - запускает все сервисы
- `-d` - в фоновом режиме
- `--build` - пересобирает образы

### 5.2. Проверка статуса

```bash
docker compose ps
```

Должны быть запущены 3 контейнера:
- `catdogs_db` - база данных (healthy)
- `catdogs_server` - API сервер
- `catdogs_client` - фронтенд

### 5.3. Просмотр логов

```bash
# Все сервисы
docker compose logs -f

# Конкретный сервис
docker compose logs -f server
docker compose logs -f client
docker compose logs -f postgres
```

### 5.4. Проверка работы

```bash
# Проверка API
curl http://localhost/api/status

# Должен вернуть: {"message":"ok"}
```

## 🌐 Шаг 6: Настройка домена и SSL (опционально, но рекомендуется)

### 6.1. Настройка DNS

В настройках вашего домена добавьте A-запись:
```
@    A     ваш_ip_адрес
www  A     ваш_ip_адрес
```

### 6.2. Установка Nginx на хост (для проксирования)

```bash
apt install nginx certbot python3-certbot-nginx -y
```

### 6.3. Создание конфигурации Nginx

```bash
nano /etc/nginx/sites-available/catdogs
```

Вставьте:

```nginx
server {
    listen 80;
    server_name ваш_домен.com www.ваш_домен.com;

    location / {
        proxy_pass http://localhost:80;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Активируйте:

```bash
ln -s /etc/nginx/sites-available/catdogs /etc/nginx/sites-enabled/
nginx -t  # Проверка конфигурации
systemctl reload nginx
```

### 6.4. Получение SSL сертификата

```bash
certbot --nginx -d ваш_домен.com -d www.ваш_домен.com
```

Следуйте инструкциям. Certbot автоматически обновит конфигурацию Nginx.

## ✅ Шаг 7: Проверка работы

1. Откройте браузер: `http://ваш_ip` или `https://ваш_домен.com`
2. Проверьте API: `http://ваш_ip/api/status`
3. Попробуйте добавить картинку в избранное

## 🔧 Полезные команды

```bash
# Остановить все контейнеры
docker compose down

# Перезапустить контейнеры
docker compose restart

# Пересобрать и перезапустить
docker compose up -d --build

# Просмотр логов
docker compose logs -f

# Войти в контейнер
docker compose exec server sh
docker compose exec postgres psql -U postgres -d catdogs

# Очистка неиспользуемых образов
docker system prune -a
```

## 🔒 Безопасность

1. **Измените пароль базы данных** в `.env` на надежный
2. **Настройте файрвол:**
```bash
ufw allow 22/tcp   # SSH
ufw allow 80/tcp   # HTTP
ufw allow 443/tcp  # HTTPS
ufw enable
```

3. **Не коммитьте `.env` файл в Git** (он уже в .gitignore)

## 🐛 Решение проблем

### Проблема: "Port already in use"
```bash
# Найдите процесс на порту
lsof -i :80
# Убейте процесс или измените порт в docker-compose.yml
```

### Проблема: База данных не подключается
```bash
# Проверьте логи
docker compose logs postgres
# Проверьте переменные окружения
docker compose exec server env | grep DATABASE
```

### Проблема: CORS ошибки
- Убедитесь, что `CLIENT_URL` в `.env` правильный
- Проверьте настройки CORS в `server/serverConfig.js`

### Проблема: Миграции не выполняются
Миграции выполняются автоматически при старте сервера через `entrypoint.sh`.
Если нужно выполнить вручную:
```bash
docker compose exec server sh -c "cd /app && npx sequelize-cli db:migrate"
```

## 📝 Резюме

После выполнения всех шагов ваше приложение будет доступно по адресу:
- **HTTP:** `http://ваш_ip` или `http://ваш_домен.com`
- **HTTPS:** `https://ваш_домен.com` (если настроили SSL)

Все сервисы работают в Docker контейнерах и автоматически перезапускаются при перезагрузке сервера.

